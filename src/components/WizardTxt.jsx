function WizardTxt({}) {
  // const currentWeather = days[date.currentDay][date.timeOfDay];

  return (
    <div>
      <div className="wizard-bubble">Placeholder text.</div>
    </div>
  );
}

export default WizardTxt;
